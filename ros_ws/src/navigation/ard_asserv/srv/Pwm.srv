# Send a PWM command, left and right are the PWM applied on the wheels (limited to 255) and duration is in s
int16 left # -255 : 255
int16 right # -255 : 255
float32 duration # s
bool auto_stop # Stops when blocked
---
bool response